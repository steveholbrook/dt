{
  "name": "downtime-tracker-lite",
  "version": "1.4.0",
  "private": true,
  "scripts": {
    "serve": "http-server -p 8080 -c-1 .",
    "test:e2e": "playwright test",
    "test:install": "npx playwright install --with-deps"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "@playwright/test": "^1.48.2"
  }
}